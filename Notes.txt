FastAPI uses ASGI server i.e. Asynchronize Server Gateway Interface.

Pydantic Models only knows how to deal with the dictionary and SQL.

Schema/Pydantic Models is to ensure that it is requesting what is provided by the user and in return response is given.

SQLalchemy models is for handling and forming a database table like CRUD application table in database.

In schema, we can also define a class that just need a specific fields for the action/response.

models = this file has the table format for the database.
schemas = this file have pydantic model which has the format of data in which we have pass.
database = this file contains information and connects the local databse with API.

JWT authentication token is for returning the json file in a particular encoded format.

How to filter from the two tables in one with the help of JOIN :-

SELECT posts.*, COUNT(votes.user_id) as no_of_votes FROM posts LEFT JOIN votes ON posts.id = votes.post_id group by posts.id;

CORS = Cross-Origin Resourse Sharing
meaning the frontend running in browser request the backend to communicate and the backend is in different origin than frontend.